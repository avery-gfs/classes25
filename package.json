{
  "scripts": {
    "push": "git add . && git status && git commit -m 'update' && git push",
    "fmt": "npx prettier --write --prose-wrap always .; ruff format . --no-respect-gitignore"
  },
  "dependencies": {
    "prettier": "^3.6.2"
  }
}
