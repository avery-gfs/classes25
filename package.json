{
  "scripts": {
    "push": "git add . && git status && git commit -m 'update' && git push",
    "fmt": "npx prettier --write --prose-wrap always ."
  },
  "dependencies": {
    "prettier": "^3.6.2"
  }
}
