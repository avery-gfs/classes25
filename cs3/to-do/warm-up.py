# Define a variable `name` to a string containing your name

# Defint a variable `tasks` to contain an empty list

# Make a loop that prints "hello" forever (then comment it out afterwards)

# Print out "Hello <name>" based on the value in the name variable

# Get a name string from the user as input and print out "Hello <name>"

# Clear the console

# Write code to get a team name as user input and print out `True` / `False`
# depending on whether the team is in the list `teams`

teams = ["ravens", "eagles", "bills", "lions"]

# Write code to add a team name to `teams`

# Write code to remove a team name from `teams`
